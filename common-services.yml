version: "2"

services:
  was:
    environment: 
    - "system.domain=${MACHINE_HOST}"
    - "db.Url=${DB_URL}"
    - "db.Password=${DB_PASSWORD}"
    - "db.Username=${DB_USERNAME}"
    - ARCHIVE_FILE
    - JAVA_MAX_SIZE
    - SYSTEM_TIMEZONE